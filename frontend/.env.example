# Optional: point the frontend at a backend API.
# If unset, the app will use an in-memory sample dataset.
REACT_APP_API_BASE=http://localhost:8000
# Alternative name supported (fallback):
# REACT_APP_BACKEND_URL=http://localhost:8000
